# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/edmeca_dev

# Session Configuration  
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Authentication (Optional - for Replit auth integration)
ISSUER_URL=https://auth.replit.com
REPL_ID=your-repl-id

# Email Configuration (Optional - for Gmail integration)
# Configured through Replit integrations when hosted on Replit

# Development Configuration
NODE_ENV=development
PORT=5000